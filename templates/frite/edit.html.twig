{% extends 'base.html.twig' %}

{% block title %}Edit Frite{% endblock %}

{% block body %}
    <h1>Edit Frite</h1>

    {{ include('frite/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('frite_index') }}">back to list</a>

    {{ include('frite/_delete_form.html.twig') }}
{% endblock %}
